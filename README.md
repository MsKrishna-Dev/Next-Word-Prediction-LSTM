# Next-Word-Prediction-LSTM
Developed a deep learning–based Next Word Prediction system using NLP and LSTM networks. The model learns word sequences from text data and predicts the most probable next word. Implemented using TensorFlow and Keras with custom tokenization and sequence generation.
